# In production change name to .env
# SMTP
SMTP_HOST="mail.server.com"
SMTP_USER="no-reply@server.com"
SMTP_PASS="secret_password"
SMTP_PORT=465

# Emails
DESTINATION_EMAIL="destination@server.com"
FROM_EMAIL="no-reply@server.com"
FROM_NAME="Web server.com"

# Turnstile
TURNSTILE_SECRET="SECRET-TOKEN"
